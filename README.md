# FreeCAD Polar coordinates exporter

## Installation

Put PolarCoordinatesExporter folder to `~/.FreeCAD/Mod/`

## Export options

To change default export options go to `Edit -> Preferences -> Import-Export -> Polar coordinates (Tab)`
